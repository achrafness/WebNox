#######################################
# WebNox Configuration Example
# Copy this file to .env and modify as needed
#######################################

# ===========================================
# SERVER CONFIGURATION (IMPORTANT!)
# ===========================================
# The hostname, domain, or IP address where labs will be accessible
# This is the URL shown to users when they start a lab instance
# Examples:
#   - For local development: localhost
#   - For server with IP: 192.168.1.100 or your.public.ip
#   - For domain: labs.yourdomain.com
LAB_HOST=localhost

# The port where the main WebNox application runs
APP_PORT=5000

# ===========================================
# APPLICATION SETTINGS
# ===========================================
# Generate a secure key: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=change-this-to-a-secure-random-string

# Environment: development, production
FLASK_ENV=production
DEBUG=false

# ===========================================
# DATABASE
# ===========================================
# SQLite (default)
DATABASE_URL=sqlite:///instance/webnox.db

# PostgreSQL (for production)
# DATABASE_URL=postgresql://user:password@localhost:5432/webnox

# ===========================================
# DOCKER CONFIGURATION
# ===========================================
DOCKER_HOST=unix:///var/run/docker.sock
LAB_NETWORK=webnox-labs

# ===========================================
# LAB INSTANCE SETTINGS
# ===========================================
# Port range for lab containers (make sure these are open in firewall)
LAB_PORT_START=10000
LAB_PORT_END=20000

# Lab instance timeout in seconds (default: 2 hours)
LAB_INSTANCE_TIMEOUT=7200

# ===========================================
# REDIS (Optional - for session management)
# ===========================================
REDIS_URL=redis://localhost:6379/0

# ===========================================
# SSL/TLS (Optional - for HTTPS)
# ===========================================
# SSL_ENABLED=true
# SSL_CERT_PATH=/etc/ssl/certs/webnox.crt
# SSL_KEY_PATH=/etc/ssl/private/webnox.key

# ===========================================
# SMTP (Optional - for email notifications)
# ===========================================
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your-smtp-user
# SMTP_PASSWORD=your-smtp-password
# SMTP_FROM=noreply@webnox.local
# SMTP_TLS=true
